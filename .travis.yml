language: node_js
nodejs:
  - 8

addons:
  chrome: stable

branches:
  only:
  - /^v\d+\.\d+\.\d+$/

deploy:
  provider: pages
  skip-cleanup: true
  local-dir: dist
  keep-history: true
  github-token: $GITHUB_TOKEN
  verbose: true
  on:
    tags: true
